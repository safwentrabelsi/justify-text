{
  "name": "justify-text",
  "version": "1.0.0",
  "description": "back end test ",
  "main": "server.js",
  "scripts": {
    "client-install": "npm install --prefix justify-front",
    "start": "node server.js",
    "server": "nodemon server.js",
    "client": "npm start --prefix justify-front",
    "heroku-postbuild": "cd justify-front && npm install --only=dev && npm install && npm run build"
  },
  "author": "safwen",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "cors": "^2.8.4",
    "express": "^4.16.3",
    "jsonwebtoken": "^8.3.0",
    "nodemon": "^1.18.4",
    "path": "^0.12.7"
  },
  "engines": {
    "node": "8.11.3"
  }
}
